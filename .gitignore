*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
build/
/captures/
.externalNativeBuild/
.cxx/
local.properties
.kotlin/
.agents/
.claude/
*.disabled
/scripts/[0-9]*_*.ps1

*.hprf

*.cmd
# llama.cpp (GGUF) backend — machine-local build output, copied in by the syncLlamaCppLibs
# Gradle task from whatever local.properties' llamacpp.dir points at. Not source, not portable
# across machines/ABIs, so not committed (unlike the LiteRT-LM accelerator .so files already
# checked into this same directory, which ARE meant to be committed).
/app/src/main/jniLibs/armeabi-v7a
/app/src/main/jniLibs/arm64-v8a

brag-output
docs/mockup
app/release